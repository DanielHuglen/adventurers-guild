version: "3.8"

services:
  adventurers-guild:
    image: danielhuglen/adventurers-guild:latest
    container_name: adventurers-guild
    restart: always
    ports:
      - "4000:8081"
    volumes:
      - app_data:/app/data

volumes:
  app_data: